# Authored v3 fixtures

These are exact copies of Workflow 1's frozen v3 contract fixtures and its fixed question.png. They are synthetic contract/storage tests, not a course trial, external-provider output, verified model inspection log, or learning-quality judgment.

Unit tests use an explicitly injected raster decoder seam. Header/signature/CRC/digest checks use actual PNG bytes. Actual browser image decoding, IndexedDB persistence, zoom, source neutrality and fresh-profile bundle restore remain separate integration acceptance work.
