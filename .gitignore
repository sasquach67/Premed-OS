# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env / secrets (keep .env.example tracked)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Nested app build/reference folders
premed-hq/node_modules/
premed-hq/dist/
premed-hq/mockups/

# Atlas is its own git repository. Committing it produced a broken gitlink
# (mode 160000 with no .gitmodules), which clones as an empty folder.
# If it should travel with this repo, add it deliberately:
#   git submodule add <url> Atlas
Atlas/

# Archives and scratch exports — the mockups they contain are already
# tracked as real files in premed-hq-documentation/ and mockup-lab/.
*.zip
ziXi8K6N

# Base64 intermediates from building the mockup portraits. The images are
# inlined in the HTML; these are the leftovers.
*.b64

# Local editor/session config
.claude/
premed-hq/.claude/
