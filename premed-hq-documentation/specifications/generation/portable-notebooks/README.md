# Portable notebook instruction builder — draft 2

Canonical Markdown owns learning rules. This directory provides reproducible assembly and deterministic structural checks, not a second prose rule set. Generated copy prompts contain the applicable rule text, editable class/request input, and the exact v2 JSON Schema. No real class source was inspected or trial generated by this tooling.

## Build and verify

Run from the canonical repository root. The output may be any separate directory. Python standard library is sufficient for building; verification uses the independently maintained `jsonschema` Draft 2020-12 implementation.

```sh
python3 premed-hq-documentation/specifications/generation/portable-notebooks/build_prompts.py --canonical-root . --output /tmp/notebook-instruction-output
python3 premed-hq-documentation/specifications/generation/portable-notebooks/build_fixtures.py --output /tmp/notebook-instruction-output
python3 premed-hq-documentation/specifications/generation/portable-notebooks/build_feasibility_case.py --output /tmp/notebook-instruction-output/feasibility-case
python3 premed-hq-documentation/specifications/generation/portable-notebooks/build_conversation_examples.py --output /tmp/notebook-instruction-output
python3 premed-hq-documentation/specifications/generation/portable-notebooks/build_revision_cases.py --output /tmp/notebook-instruction-output/revision-case
python3 premed-hq-documentation/specifications/generation/portable-notebooks/build_standardization.py --canonical-root . --output /tmp/notebook-instruction-output
python3 -m venv /tmp/notebook-validation
/tmp/notebook-validation/bin/python -m pip install -r premed-hq-documentation/specifications/generation/portable-notebooks/requirements-validation.txt
/tmp/notebook-validation/bin/python premed-hq-documentation/specifications/generation/portable-notebooks/check_package.py --canonical-root . --output /tmp/notebook-instruction-output
```

`build_schema.py` reconstructs the adjacent schema. That file is an exact snapshot of the coordinator's agreed v2 contract; change it only in coordination with the app owner. `validate_package.py --schema PATH PACKAGE...` validates supplied JSON files separately. It rejects unresolved/cross-entry references, inconsistent excerpt ownership, invalid source-use claims, unsupported mastery links, broken revision arithmetic, mismatched table widths, and selected learning-shape violations. It cannot verify actual source text, entailment, comprehensive coverage, learning quality or UI behavior.

`canonical-manifest.json` records canonical paths, full source hashes, selected-fragment hashes, tooling hashes, output hashes, and deliberate extensions. `prompt-composition.json` gives the app the exact tokens and substitution contract. `validation-report.json` records checks and limits.

## Rule selection

- All of `19-study-source-and-format-contract.md`, `20-external-notebook-workflow.md` and the canonical request envelope in `21-external-notebook-request-template.md` appear in every prompt. The goal-label and prompt-build slots in 21 are fixed at build time; eleven student-input slots remain. The visible prompt build is notebook-instructions-beta-6; the unchanged transport instructionsVersion is notebook-workflows-draft-2.
- Every goal also includes applicable global rules from 02: sections 1.1–1.8, clarification/background boundaries, source primacy, cross-source behaviors and thin-source handling.
- Review includes the full Runtime briefing mirror and Required structure from 03, its Portable review notebook section, the introduction and every Rules row from 11, its Portable objective coverage section, and applicable visual-learning sections from 06. All included fragments are verbatim; see METHODOLOGY-AUDIT.md for the section-level audit and explicit portable differences.
- Assessment and assignment each include every substantive `NA-` or `NW-` rule verbatim and the corresponding portable section. Persistence/recovery and renderer implementation paragraphs are excluded.
- Legacy transport terms in these verbatim rules are explicitly mapped in canonical 20, including clarification references, diagrams, contradictions and emphasis metadata. The v2 schema governs emitted fields. For example, absent objective evidenceLimit is encoded as null, practice uses prompt/answer/rationale with excerpt IDs, and TITLE becomes entry.title.
- Class preferences are explicit editable request input. No rule preset is inferred from BIOL103, ANTH147, PSYC101, ENGL105 or GEOG121; those are Andy's manual material/trial environments.

## App integration contract

Maintain exactly three prewritten goal templates derived from canonical Markdown. Goal selection retrieves the chosen static template; no AI API generates or rewrites prompts. Local placeholder insertion and student notes are plain-text operations. The student subsequently uses that prompt in their preferred AI to generate notebook content.

Load the generated template for the selected goal. Replace its eleven literal `{{TOKEN}}` values exactly once with JSON-encoded string values (or null where unknown). CLASS_PREFERENCES defaults to an empty string. Use a regex replacement callback or equivalent single-pass algorithm; do not repeatedly replace tokens in already inserted user text. `compose()` in build_prompts.py is a reference implementation. JSON-encoding handles quotes and newlines; it does not turn source text into instructions or give access to named attachments.

The completed prompt string is the single value for preview, clipboard and UTF-8 Markdown download. Keep each surface byte-identical. Selected saved-file titles and user-listed attachments can populate MATERIALS, but the app must explain that the original material still needs to be supplied to the chosen AI. SCOPE may include the selected lessons and scope authority. Existing exported JSON can populate or be identified in REVISION_INPUT; naming it is not reading it.

The output package format is `premed-os-notebook-package`, version 2, instructionsVersion `notebook-workflows-draft-2`. Keep the schema unchanged unless both development workflows agree a change is necessary. Template snapshots are generated assets: change canonical Markdown, regenerate, inspect the manifest/diff, and rerun checks rather than hand-editing prompt assets.

## Review limits and acceptance

The ten general example packages use an invented route workshop, not actual course knowledge. A separate manual feasibility case adds staged checkpoints, a long invented Week 2 packet, missing lessons and a complete partial-preparation JSON; it is an expected behavior case, not an executed AI trial or a capacity benchmark. See FEASIBILITY.md for per-goal inputs, outputs and limits. They include three goal examples and missing-objective, multi-lesson assessment, stage-limited assignment, gap-only review partial-review, all-unreadable-material and exact-revision edges. Scope-only excerpts do not supply answers; unreadable and not-accessed sources remain in inventory; missing objectives remain in requirements without fake mastery objects. The hint fixture includes a targeted hint and leaves the final deliverable outside scope. Its planning requirement remains partial because the student's choices are still absent.

A readable-content author review checked the invented assertions against the printed passages, distinct teaching versus retrieval, scenario-dependent answer/rationale agreement, source role boundaries, and hint-stage limits before the final structural receipt. That review is neither an independent pedagogical audit nor acceptance evidence for Andy's classes. Deterministic checks do not establish that an AI followed these instructions on real material.

The coordinator owns the manual trial register. Five review trials plus at least one assessment and one assignment need Andy's clarity, depth, usefulness and class-fit ratings of at least 4/5 each, with no unresolved factual or fabricated-source errors, and the separately required app behavior proof. Do not invent ratings or mark this draft approved. Do not modify the coordinator's README or trials register from this workflow.

## External conversation contract — beta 4 foundation

Canonical 20 retains beta-2 evidence/coverage and beta-3 conversation behavior except that beta 4 explicitly supersedes automatic final export. In a normal AI chat, supplied readable material is sufficient; saved projects/folders are optional. Prepare the actual content using known needs/preferences with few consequential questions, then show an accessible readable draft and concise scope/depth/stage/source/gap review. Wait for explicit confirmation before final notebook JSON. Substantive changes need updated review and renewed confirmation; serialization preserves approved content and returns to review if validation requires a substantive correction. This is expected AI behavior, not an app state machine or a compliance guarantee. The v2 schema and eleven request fields remain unchanged.

`conversation-examples.json` contains fourteen authored expected scenarios, rendered into EXPECTED-CONVERSATIONS.md. The original nine scenarios cover the confirmation gate and conversation behavior; five beta-5 additions cover topic revision, absent/ambiguous correction context, stale saved baselines, ordinary Plus mixed materials and multi-message upload collection. Actual short hint drafts and a complete readable counterpart of the existing partial assessment fixture make review inspectable. Invalid/truncated repair samples remain in conversation-case-files/, outside valid-import globs. These files are not extra prompts or executed model trials; no ratings are prefilled.

Published beta-2, beta-3, beta-4 and beta-5 snapshots retain their original identities and hashes. Their optional preservation checks run when each archive is present (a fresh staging directory without the four historical archives has four fewer checks). Beta-3 app verification does not establish the new beta-4 conversation behavior or asset parity. Coordinator-owned integration and real trials are separate acceptance steps.

## Current beta 5: saved topic revision and ordinary-chat intake

Canonical 20 implements the topic-revision framework: inspect the exact last-saved current-content export, preserve unchanged content/IDs/student edits, honestly reuse retained excerpts, request missing old context only when needed, consolidate lecture overlap, contextualize corrections/conflicts, update teaching/practice/objective/coverage dependencies and separate distinct new topics. Show actual revised content and a concise change review, wait for explicit confirmation, then serialize the complete resulting proposal without regenerating unchanged material. External confirmation does not authorize app acceptance or history/progress operations.

`revision-context.json` records the exact agreed metadata string for the existing REVISION_INPUT token and baseline filename notebook-update-baseline.json. App target/fingerprint/history/progress remain local. A complete same-course proposal can revise the selected target and include explicitly authorized genuinely new topic IDs. App ownership includes explicit same-entry acceptance, conflict checks and recoverable study records; actual app verification is a separate receipt. Unaffected self-reported practice requires exact relevant dependency comparison; no model-generated correctness/mastery or transfer guarantee.

`build_revision_cases.py` creates authored baseline/current/revised/ambiguous-link cases and a full readable draft in revision-case/. Focused audits reject overwritten student reminders, unrelated rewrites, altered historical quotes, false rereads, stale timing answers/objectives/coverage, invented results and duplicated repetition even when schema-valid. The mapping path is isolated and unchanged; timing dependencies change. Do not import app-records-not-in-current-export.json or glob the case directory as valid packages. Prior general fixtures and schema remain unchanged.

EC-MATERIALS distinguishes direct files/images/paste and actual visual access from filenames, extracted digital text, scanned pages, OCR, ZIPs and folder access. The dated ORDINARY-CHAT-MATERIALS.md cites official provider guidance separately from permanent method rules. EC-INTAKE tracks announced multi-message upload batches until explicit done, without premature final drafts or repeated per-batch approval requests. Intake completion is separate from actual draft confirmation. The 50-image 20/20/10 scenario is hypothetical, not a provider limit; actual authored receipts expose the missing/unreadable/pending distinctions. No actual AI/Plus trial or student rating is generated.

## Beta 6 standardization — release paused

Shared20 and request21 now standardize course/target/scope checks, observable incompleteness, stable actual-inspection ledgers, precise visual uncertainty, source-command boundaries, approved-draft parity, checkpoint recovery, useful evidence-supported goal content, separate check layers and complete provider-neutral packaging. Collection-done and approval of the current actual draft remain distinct. The companion chat summary names actual concepts, question/problem types, scope/depth/emphasis and important gaps or update changes; optional discussion stays grounded in that content. The full readable draft stays accessible. Equivalent clear approval permits JSON; delivery, a question or agreement only to one edit does not. No reading or accuracy claim follows from a summary.

`build_standardization.py` builds ONE small synthetic cross-provider packet with a shared checklist, nine meaningful negative/recovery/revision cases and fixed authored PNG assets. It also reports prompt bytes/words, beta-5 deltas, component contributions, exact paragraph repetition and standalone dependencies. No provider/context run, capacity claim, real class trial or rating is recorded. All applicable methodology and the complete schema remain embedded in each of the same three prompts; the eleven runtime tokens and revision context are unchanged.
