# Authored visual-contract reference

This entire reference is fictional, non-empirical UI test material. It is not a course reading, actual scientific result, provider-produced notebook, student grade or approval. The times, scores, categories and workflow below exist only to make deterministic renderer/contract fixtures inspectable. Do not copy these examples into an actual class notebook as course evidence.

## Comparison

In this fictional archive exercise, a Text card has a written label and a written description; it has no source picture. An Image card has a written label and a supplied source picture; its description names what is visible. Both card types keep the reference ID. Compare the same dimensions: label, representation and reference identity.

## Decisions

To route a card, first ask whether its label is readable. A readable label goes to the labeled collection. An unreadable label goes to manual review. Manual review is a terminal state for this diagram; do not invent an automatic successful outcome.

## Cause and effect

The fictional archive's access switch controls whether a card can be opened. When the switch is disabled, opening is blocked. When it is enabled, the open action can display the card. This is an explicitly stipulated software rule, not a measured effect. Being labeled happens earlier in the example sequence but does not by itself cause access permission.

## Hierarchy

The fictional collection contains Text cards and Image cards. A Text card contains its label and written description. An Image card contains its label and supplied picture. These are containment relationships, not a ranking of importance or evidence quality.

## Image annotation reference

The separate authored-classroom-task.png is an unchanged existing UI fixture. It visibly states that a triangle maps to LEFT and a circle maps to RIGHT, and shows an outlined triangle beside four unmarked answer choices. Its printed PSYC 101 course label is fictional fixture context. A neutral label may identify the top corner of the displayed shape. Use the actual inspected point from IMAGE-INSPECTION.json, not guessed image coordinates. A label saying the correct response is LEFT is solution content and must not be shown as a neutral initial question stimulus.

## Timeline

The fictional scheduled session starts at 09:00 with Collect. At 09:10 it reaches Label. At 09:25 it reaches Check. On a numeric timeline measured in minutes after 09:00, these scheduled positions are 0, 10 and 25. An ordinal timeline may show Collect before Label before Check, but its spacing must not claim equal elapsed intervals. These are illustrative planned times, not empirical observations or proof that one event causes the next.

## Set overlap

The fictional set Text-described contains Card A and Card C. The set Image-supported contains Card B and Card C. Therefore Card A belongs only to Text-described, Card B only to Image-supported, and Card C to their overlap. No relative population size, area, probability or prevalence is specified. Use a two-set membership diagram with no quantitative area claim.

## Sequence strip

For the authored classroom task, the ordered instructions are: Read the written rule; Inspect the displayed shape; Apply the rule to choose a response. The second step may display the actual unchanged source image. The initial sequence must not disclose the chosen response. These are ordered task actions, not invented intermediate scientific pictures or measured durations.

## Worked example

Problem: The fictional schedule reaches Label at 09:10 and Check at 09:25. How many minutes are scheduled between them? The solution identifies the same-hour minute values 10 and 25, subtracts 25 minus 10, and concludes 15 minutes. Reveal all solution steps and the final answer together after the problem. No answer belongs in the collapsed panel title, initial alt text or neutral setup.

## Ordinal continuum

The fictional archive's readiness stages are Unsorted, Labeled and Checked, in that order. A folder can be at the Labeled stage. No equal interval, numeric score, scientific threshold or time-to-completion is established by these categories. Labeling a point on this ordinal continuum must not invent one.

## Numeric scale

A separate fictional display test uses an explicitly defined score from 0 to 10 points, with anchors at 0, 5 and 10 and a sample marker at 7. Units are points. This is an invented UI test scale, not a learner score, actual measurement or empirical distribution. A proportional numeric axis can encode these provided test values; it must not add unprovided uncertainty intervals, thresholds or observations.
